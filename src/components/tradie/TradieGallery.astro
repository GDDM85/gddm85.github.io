---
import { galleryItems } from '../../data/tradieData';
import { imageExists } from '../../utils/checkImage';

const items = galleryItems.map((item) => ({ ...item, exists: imageExists(item.path) }));
---
<section id="projects" class="bg-white py-20">
  <div class="mx-auto max-w-6xl px-6">
    <div class="mx-auto max-w-2xl space-y-3 text-center">
      <p class="text-sm font-bold uppercase tracking-[0.2em] text-leaf-600">Recent Projects</p>
      <h2 class="text-3xl font-bold text-slate-900 sm:text-4xl">A few jobs from around the area.</h2>
    </div>

    <div class="mt-12 grid gap-6 sm:grid-cols-3">
      {items.map((item) => (
        item.exists ? (
          <img src={item.path} alt={item.alt} class="aspect-square w-full rounded-2xl object-cover shadow-md" />
        ) : (
          <div class="flex aspect-square w-full flex-col items-center justify-center gap-2 rounded-2xl border-2 border-dashed border-slate-300 bg-slate-50 p-4 text-center text-slate-400">
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="h-8 w-8"><rect x="3" y="3" width="18" height="18" rx="2"></rect><circle cx="8.5" cy="8.5" r="1.5"></circle><path d="M21 15l-5-5L5 21"></path></svg>
            <p class="text-xs font-medium">public{item.path}</p>
          </div>
        )
      ))}
    </div>
  </div>
</section>
